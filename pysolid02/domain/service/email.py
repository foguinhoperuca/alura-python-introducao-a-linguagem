class EmailService:
    @staticmethod
    def send(recipient: str, subject: str, message: str) -> None:
        print(f'[TODO] Sending e-mail for {recipient}')
        print(f'[TODO] With subject: {subject}')
        print(f'[TODO] Message: {message}')
